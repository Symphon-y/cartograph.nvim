# Test fixtures

Placeholder for the minimal Vue + .NET sample workspace used to validate the
cross-stack HTTP bridge end-to-end (see CARTOGRAPH_PLAN.md §6). This is filled
in during phase 3 (adapters + http resolver); phase 1 only needs the pure-Lua
engine specs, which require no fixture workspace.
